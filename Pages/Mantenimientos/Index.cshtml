@page
@model HospitalDeVehiculosUltimaVersion.Pages.Mantenimientos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Mantenimiento[0].FechaProgramada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mantenimiento[0].FechaEjecucion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mantenimiento[0].Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mantenimiento[0].FechaRegistro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mantenimiento[0].UltimaActualizacion)
            </th>
            <th>
               Placa Vehiculo
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Mantenimiento) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaProgramada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaEjecucion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UltimaActualizacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVehiculoNavigation.Placa)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a> |
                    <a class="btn btn-sm btn-primary"
                       asp-page="/Mantenimientos/TipoPago"
                       asp-route-id="@item.Id">Procesar Pago</a>
                    <a class="btn btn-sm btn-primary"
                       asp-page="/Servicios/Index"
                       asp-route-id="@item.Id">Ver Servicios</a>

            </td>
        </tr>
}
    </tbody>
</table>
